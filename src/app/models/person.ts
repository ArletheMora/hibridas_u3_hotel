import { Timestamp } from "rxjs";

export class Person {
    id?: string;
    name: string;
    phone: string;
    fechaInicio: string;
    fechaFin: string;
    habitacion: string;
    token: number;
    pay: number;
}